# Gerado por fernando-moretes/platform. Não edite aqui: mude no orquestrador e
# rode `make rollout`. É o que o Backstage (backstage.home.lab) lê deste repo.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: platform-workflows
  description: "Workflows reutilizaveis das pipelines"
  annotations:
    github.com/project-slug: fernando-moretes/platform-workflows
    # URL da arvore com barra final: e dela que o TechDocs resolve `dir:.`;
    # sem a barra ele sobe um nivel e pede api.github.com/repos/<dono> (404).
    backstage.io/source-location: url:https://github.com/fernando-moretes/platform-workflows/tree/main/
    sonarqube.org/project-key: platform-workflows
    # TechDocs: mkdocs.yml + docs/ do proprio repositorio (rolados pelo platform)
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fernando-moretes/platform-workflows/actions
      title: Actions
      icon: github
    - url: https://sonarqube.home.lab/dashboard?id=platform-workflows
      title: SonarQube
    - url: https://defectdojo.home.lab/product?name=platform-workflows
      title: DefectDojo
  # tags so aceitam [a-z0-9-]: ci-python.yml (ci-node.yml) tem ponto e reprovava
  # todo componente na policy check do Backstage.
  tags: [other, python]
spec:
  type: other
  lifecycle: production
  owner: group:plataforma
  system: homelab
